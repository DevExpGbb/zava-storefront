name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  apm-audit:
    name: APM audit (required)
    # Pinned to @v5.0.0 reusable workflow (inline shell apm install/audit) while
    # we debug a "workflow was not found" parse error from the @v5.0.1 reusable
    # which migrated to microsoft/apm-action@v1. Plugin pins are still v5.0.1 —
    # the upgrade story (REVIEW phase split) is fully demonstrated via apm.yml.
    uses: DevExpGbb/zava-agent-config/.github/workflows/apm-audit.yml@v5.0.0

  build:
    name: Build & test (placeholder)
    runs-on: ubuntu-latest
    needs: apm-audit
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder build
        run: |
          echo "::notice::Skinny demo scaffold — no real Next.js source yet."
          echo "Once the storefront is fleshed out, restore pnpm install/lint/test/build here."
